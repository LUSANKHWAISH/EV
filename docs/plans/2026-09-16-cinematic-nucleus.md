# E.V. cinematic nucleus and interface — overnight plan

Started: 16 September 2026, approximately 02:00 IST.
Morning checkpoint: 16 September 2026, 09:00 IST (03:30 UTC).
Status: FIRE-GOLD REFRESH COMPLETE. The user accepted the candidate and requested
more activity and brighter reference-like gold/fire color. The cinematic interface
is now the project GUI default, with a classic fallback. The wider roadmap remains
with the user. This file is the durable work log and continuation entry point.

Latest follow-up: the requested reference-based flow and startup projection are
complete. See `docs/plans/2026-09-16-core-flow-and-projection.md` and its report.

## Authorized outcome

Build a substantially closer JARVIS-inspired real-time nucleus and a complete,
cinematic E.V. interface. Save the plan first, then work autonomously overnight.
The user will supply the wider project roadmap and music-mode requirements later.
No TTS/PCM work, music playback, backend rewrite, installer or external publication
is included. The earlier blanket no-edit restriction has been superseded by this
explicit request to plan and implement. Preserve unrelated existing work.

## Visual references and lessons

- Primary motion: `F:\OneDrive\Videos\NVIDIA\Desktop\Desktop 2026.09.16 - 01.29.34.01.mp4`.
- Structural detail: `F:\OneDrive\Videos\Captures\Avengers.Age.Of.Ultron.2015.2160p.4K.BluRay.x265.10bit.AAC5.1-[YTS.MX].mkv - VLC media player 2026-09-16 01-31-37.mp4`.
- Especially useful: second clip at 21–24 seconds and 34.5 seconds, before the
  destructive attack. Exclude blue tendrils, disintegration and violent failure motion.
- Original image: `C:\Users\LUSAN\Downloads\approved design.jpeg`.
- Rejected prototype: `D:\EV-V3Lab`. Preserve its existing evidence for comparison.

The target is an open, irregular, computational volume: fine segmented circuitry,
partial moving bands, scattered bright nodes, small curling luminous center,
different layer speeds, and substantial dark gaps. The previous solid-looking
doughnut center, uniform sphere texture and smooth broad rings are rejected.
Movie frames are references only, not a rotating-image replacement or asset texture.

## Workspace and boundaries

- Implement the next candidate under `D:\EV\prototypes\cinematic_v4`.
- Reuse the installed Python/PySide6 environment at `D:\EV\.venv`.
- Reuse tested geometry/instancing/alpha techniques where useful; redesign the art.
- Keep the normal E.V. launch path available. Prepare an additive presentation
  integration only when its contracts and tests are verified; do not silently
  replace a working default with an unreviewed visual experiment.
- Preserve `core/tts.py`, authority, providers, voice processing, approvals and
  execution logic. Nucleus input emits harmless requests into existing controllers.
- Use the current `GuiBridge` / `VisualStateController` state vocabulary. Preview
  fixtures must be labelled. System telemetry may be measured locally.
- Save source hashes and Git status before work. Do not reset, clean or commit
  unrelated changes. No automatic Git commit or installer is planned.

## Implementation phases and acceptance criteria

1. **Baseline and reference study**
   - Snapshot source hashes, runtime and current display conditions.
   - Extract a small local reference sheet with exact timestamps for comparison.
   - Establish which prototype components can be reused safely.
   - Pass: reproducible environment; protected source baseline recorded.

2. **Core reconstruction**
   - Small open center made of tapered curling filaments and sparse luminous arcs.
   - Broken spherical/cylindrical circuit patches and segmented data bands, with
     independent axes, offsets and bounded distortion.
   - Sharp traces at several scales; restrained halos; hot gold accents and dark gaps.
   - GPU particles occupy the interior as well as the boundary.
   - Pass: actual rendered silhouette/detail materially closer to the video at
     standard and expanded sizes. Reject a solid torus, atom icon or wire nest.

3. **Complete cinematic interface**
   - Dark graphite stage with restrained amber highlights, fine framing and clear
     typography. Keep the response readable and the core visually dominant.
   - Real working command composer, response surface, lifecycle/task rail,
     telemetry rail, mode navigation, settings and expanded core presentation.
   - Responsive layout with usable smaller-window fallback, scrollable content,
     keyboard focus and clear states. Avoid decorative fake progress/telemetry.
   - Reserve a disabled, clearly planned music destination; no invented music features.
   - Pass: coherent 1080p screenshot, readable response, operable controls and
     no core/panel overlap at the checked sizes.

4. **State, input and performance**
   - Default 60 FPS target, lighter 30 FPS Quality profile that reduces particles,
     circuit detail, render resolution and glow work; animation pause.
   - Free yaw, pitch clamped near ±18°, bounded zoom, reset and module requests.
   - Existing controller-driven idle/listening/thinking/speaking/etc. Preview audio
     is simulation only. Clicking never approves or executes a plan.
   - Internal alpha compositing; no desktop transparency or broken viewport rectangle.
   - Pass: meaningful Qt input/render tests; no shader/QML errors; disclose display
     refresh and capture overhead. Do not claim 60 FPS from a 32 Hz session.

5. **Reviewable delivery**
   - Actual 1920×1080 screenshots, an actual 10–15 second motion recording, reference
     comparison, run launcher, documentation, tests and measured performance.
   - Recheck protected source hashes and document every intentional project edit.
   - Provide a morning status report with remaining work and limitations. Do not
     claim an objective similarity percentage or complete visual success from tests.

## Overnight operation

Continue useful work without waiting for optional clarification. Record milestones,
failures and next actions here. A same-task scheduled continuation may resume from
this file if a session ends; it must not run a second competing renderer or repeat
finished work. Finish a safe checkpoint by 09:00 IST. Stop the overnight schedule
at that time or earlier on completion/cancellation. Do not keep running expensive
benchmarks or sleeping merely to fill the night.

## Progress log

- 02:00 IST: Plan created before implementation. Repository has substantial
  pre-existing tracked/untracked work. No AGENTS.md was found in relevant roots.
- Next: preserve baseline; create the V4 candidate and reference comparison assets.
- 02:20 IST checkpoint: New candidate is rendering in `prototypes/cinematic_v4`.
  Baseline of 352 protected files recorded at Git HEAD 8237dbb. Display is back
  to BenQ 1920×1080 / 60 Hz / DPR 1.0. Rebuilt segmented data surfaces, batched
  fine routes, six broken bands, small open filament center and particles.
  Full cinematic QML interface now exists with command composer, response,
  context rail, real local telemetry, navigation, settings and session activity.
  Actual draft images: `evidence/interface_01.png`, `interface_02_expanded.png`.
  Early render issues (missing POSITION, delegate parent access, invalid Row
  anchors) were corrected. Initial runs show ~60 FPS but are not final benchmarks.
- Next: validate interactions/responsive layout/alpha, continue core art refinement,
  then prepare opt-in integration using existing GuiBridge. Do not call the current
  visuals approved. The bridge currently has no public start-listening slot; keep
  the microphone request explicit and unconnected rather than inventing authority.

## Continuation checklist

- 02:46 IST checkpoint: All 34 preview input/render checks passed, including
  actual posted Qt keyboard/mouse events, modal drawers, bounded yaw/pitch/zoom,
  30 FPS profile geometry/particle/resolution reduction, alpha and exact frozen
  pixels. Fixed the test harness to traverse Repeater visual parents and post
  asynchronous events; synchronous QtTest focus held the Python GIL against the
  render thread on this Qt build. No such hang occurs with posted/native input.
  Existing targeted regression suite: 132 passed (app, bridge, results, lifecycle,
  approvals and visual state).
  Added opt-in `python -m gui.app --cinematic`, a read-only GuiBridge adapter,
  existing provider overlay and approval overlay above the new presentation.
  Only intentional existing-file edit: gui/app.py; its full before copy is in
  evidence/gui_app_before.py.txt. Production TTS/authority/backend are untouched.
  PySide inherited notify descriptors caused a native crash during initial QML
  integration; using each subclass's own notify signals fixed it.
  Latest art draft: interface_03_expanded.png, with clustered internal routes,
  radial fragmented schematics, warped bands and fewer complete surface layers.
  Still refining visual fidelity; current images are not user-approved.
- Next: finish rendered integration/approval checks; refine luminous bands and
  motion; record actual 15-second video; benchmark both profiles without capture;
  final hash audit, launchers, documentation and morning report.
- 03:07 IST checkpoint: Actual production `gui.app --cinematic` startup passed
  with native Windows chrome, real observational telemetry and 60 FPS reported
  by the renderer. No commands, approvals, model API requests or voice playback
  were triggered during that startup check. The isolated integration suite
  passes all 22 bridge/overlay checks. Final expanded preview suite passes 39
  checks, including selectable read-only full responses and 0/90/180/270-degree
  yaw renders with transparent borders and approximately 734–758px extents.
  Small-window expand control no longer overlaps the telemetry heading.
  Created root launchers for preview and connected E.V., plus candidate README.
  A real 15-second draft capture exists; its capture overhead is disclosed.
  Reference comparison prompted a final art pass: more luminous peripheral
  schematics, fewer visible band tracks, brighter curls and adjusted band axes.
  The film remains more organic and locally bright; do not claim an exact match.
  Quality profile now avoids loading the full circuit texture for non-circuit
  materials: measured image data fell from 13.625 MiB to 6.125 MiB in Quality.
  Finishing named captures/video, final timings, source audit and review report.
- Final checkpoint: Named review images and the reference comparison have been
  refreshed for the final art pass. `cinematic_motion.mp4` is verified as an
  actual 1920×1080, 15-second, 450-frame recording; 51 duplicated capture frames
  are disclosed in its JSON. Final unrecorded averages are 59.65/59.95 FPS in
  standard/expanded 60 mode, and 29.63 FPS in both 30 mode layouts. The full
  report is saved at `docs/reports/2026-09-16-cinematic-nucleus.md`.
  No renderers need to remain running. Pause the overnight continuation once the
  final source audit has passed; keep this candidate stable for the user's review.

- Morning follow-up: Increased idle motion from 0.55 to 0.95, added band texture
  flow, accelerated internal light currents and particles, and changed the shader
  palette to ember orange, gold and ivory highlights with stronger glow. Preserved
  bounded pitch, free yaw, restrained approval/error/sleep states and lighter mode.
  `gui.app` now defaults to cinematic; `--classic` loads the original presentation.
  Added `Launch EV.cmd` and `Launch Classic EV.cmd`. Both actual startup paths pass.
  Current validation: 39 launcher tests, 39 rendered interaction checks and 22
  bridge/approval checks pass. Expanded 20-second unrecorded runs average 59.94 FPS
  and 29.62 FPS for the 60 and 30 profiles. Current media, source manifest and
  audit are in `prototypes/cinematic_v4/evidence/fire_refresh/`; report is
  `docs/reports/2026-09-16-fire-gold-refresh.md`. No voice/backend changes.

1. Read this log and inspect the current candidate/report before changing anything.
2. Check whether another lab renderer or benchmark is running; avoid overlap.
3. Continue the next incomplete phase, not a fresh prototype from scratch.
4. Update the log before ending a session and provide a truthful status.
